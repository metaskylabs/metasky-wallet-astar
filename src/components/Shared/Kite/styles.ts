import { css } from '@emotion/react';
import utils from '@styles/shared/utils';

export const loader = css({
  width: `${utils.remConverter(192)}`,
  height: `${utils.remConverter(156)}`,
  borderRadius: `100%`,
  marginBottom: `${utils.remConverter(12)}`,
});
