import { padding } from '@styles/Modules/details';
import { colors, typography } from '@styles/shared';
import { css } from '@emotion/react';

export const buttonContainer = css({
  padding: `5px`,
});
