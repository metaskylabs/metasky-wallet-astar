import { css } from '@emotion/react';
import { utils } from '@styles/shared';

export const shimmerCard = css({
  height: utils.remConverter(748),
  width: `100%`,
  backgroundSize: `1200px 100%`,
  borderRadius: utils.remConverter(4),
});
