import { FullScreenKiteLoader } from '@components/Shared';

const NearLoading = () => {
  return (
    <FullScreenKiteLoader isOpen={true}>
      Connecting Near Wallet
    </FullScreenKiteLoader>
  );
};
export default NearLoading;
