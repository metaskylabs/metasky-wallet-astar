import { css } from '@emotion/react';
import { utils } from '@styles/shared';

export const coinHeaders = css({
  padding: `0 ${utils.remConverter(16)} ${utils.remConverter(
    16,
  )} ${utils.remConverter(16)}`,
});
