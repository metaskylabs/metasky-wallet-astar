import { css } from '@emotion/react';
import { utils } from '@styles/shared';

export const container = css({
  padding: ` 0 ${utils.remConverter(24)} ${utils.remConverter(16)}`,
  height: `100%`,
});
