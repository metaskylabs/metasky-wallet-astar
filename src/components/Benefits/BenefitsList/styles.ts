import { css } from '@emotion/react';

export const benefitsCard = css({
  margin: `0 0 0 0`,
  marginLeft: 0,
});

export const emptyBenefitsContainer = css({
  display: `grid`,
  height: `70%`,
  width: `100%`,
  position: `absolute`,
});
