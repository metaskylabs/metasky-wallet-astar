export enum BenefitType {
  OWNED = `own`,
  MARKETPLACE = `marketplace`,
}

export enum WalletType {
  SKYWALLET = `skywallet`,
  METAMASK = `metamask`,
  NEAR = `near`,
}

export enum ListingType {
  DROP = `DROP`,
  MARKETPLACE = `MARKETPLACE`,
  SIGNATURE_DROP = `SIGNATURE_DROP`,
  SIGNATURE_DROP_MINATOCHAN = `SIGNATURE_DROP_MINATOCHAN`,
}

export enum NFTPlatform {
  METASKY = `metasky`,
  WALLETCONNECT_OPENSEA = `walletconnect_opensea`,
}

export enum ConnectionState {
  CONNECTED,
  DISCONNECTED,
}
