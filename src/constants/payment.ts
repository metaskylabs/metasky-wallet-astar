export enum PaymentMethod {
  METAMASK = `METAMASK`,
  CARD = `CARD`,
  UPI = `UPI`,
  SKYWALLET = `SkyWallet`,
}

export enum PaymentOption {
  WALLET = `WALLET_BALANCE`,
  METAMASK = `METAMASK`,
  FIAT = `FIAT`,
}

export enum PGTypes {
  CASHFREE = `CASHFREE`,
  STRIPE_AND_HYPTO = `STRIPE+HYPTO`,
  FREE = `FREE`,
  AIRPAY = `AIRPAY`,
}

export const ONE_LAKH = `100000`;
